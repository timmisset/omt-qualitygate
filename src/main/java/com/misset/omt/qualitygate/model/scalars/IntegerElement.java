package com.misset.omt.qualitygate.model.scalars;

import org.yaml.snakeyaml.nodes.Node;

public class IntegerElement extends AbstractScalar {
    public IntegerElement(Node node) {
        super(node);
    }
}
