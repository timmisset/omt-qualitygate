package com.misset.omt.qualitygate.model.scalars.injected;

import org.yaml.snakeyaml.nodes.Node;

public class ODTQueriesDefinition extends ODTScript {
    public ODTQueriesDefinition(Node node) {
        super(node);
    }
}
